/* ==========================================================================
   HOME PAGE — small enhancements

   Currently empty. This is the slot for anything the home page needs that
   isn't content rendering.

   The tree-of-the-day rotation is NOT here: it lives in trees.js, driven by
   the entries in trees-data.js.

   (This previously assembled the contact email from data- attributes to keep
   it out of the page source. That was removed — it only stopped the most
   naive scrapers, and the fallback text showed whenever the script hadn't
   loaded, which read as a rendering bug. The address is a plain mailto link
   in index.html now.)
   ========================================================================== */
